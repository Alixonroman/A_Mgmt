<%= form_with(model: apartment_photo) do |form| %>
  <% if apartment_photo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(apartment_photo.errors.count, "error") %> prohibited this apartment_photo from being saved:</h2>

      <ul>
        <% apartment_photo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.text_field :image_url %>
  </div>

  <div>
    <%= form.label :apartment_id, style: "display: block" %>
    <%= form.number_field :apartment_id %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
